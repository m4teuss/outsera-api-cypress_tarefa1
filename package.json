{
    "name": "api.tests",
    "version": "1.0.0",
    "private": true,
    "description": "Testes de api",
    "scripts": {
        "cy:open": "cypress open",
        "cy:run": "cypress run",
        "report:merge": "node reporter.js",
        "test": "npm run cy:run && npm run report:merge"
    }
}